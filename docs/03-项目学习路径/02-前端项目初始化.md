# 第2周：前端项目初始化 — React + TypeScript + Vite

> 前置知识：你精通 TypeScript + React，本节重点是项目架构设计和规范

---

## 1. 初始化项目

```bash
# 使用 Vite 创建 React + TypeScript 项目
npm create vite@latest frontend -- --template react-ts

cd frontend
npm install

# 安装核心依赖
npm install \
  zustand          # 状态管理（你熟悉）
  react-router-dom  # 路由
  @tanstack/react-query  # 数据请求+缓存
  tailwindcss       # 样式
  lucide-react      # 图标库
  date-fns          # 日期处理
```

---

## 2. 项目目录结构

```
frontend/
├── src/
│   ├── components/         # 通用组件
│   │   ├── ui/             # 基础UI组件（Button, Input, Card...）
│   │   └── layout/         # 布局组件（Sidebar, Header, Footer）
│   ├── pages/               # 页面
│   │   ├── Dashboard/       # 工作台/首页
│   │   ├── AgentChat/      # Agent 对话页面
│   │   ├── AgentManage/    # Agent 管理
│   │   ├── TaskMonitor/    # 任务监控
│   │   └── Settings/       # 设置
│   ├── stores/             # Zustand 状态管理
│   │   ├── agentStore.ts   # Agent 状态
│   │   ├── chatStore.ts    # 聊天状态
│   │   └── taskStore.ts    # 任务状态
│   ├── services/           # API 调用层
│   │   ├── api.ts         # 封装 fetch
│   │   ├── agent.ts        # Agent 相关 API
│   │   └── task.ts        # 任务相关 API
│   ├── hooks/              # 自定义 Hooks
│   │   ├── useAgent.ts
│   │   └── useWebSocket.ts
│   ├── types/              # TypeScript 类型
│   │   └── index.ts
│   ├── App.tsx
│   └── main.tsx
├── public/
├── package.json
└── vite.config.ts
```

---

## 3. 核心类型定义

```typescript
// types/index.ts

// Agent 相关
export interface Agent {
  id: string;
  name: string;
  description: string;
  model: string;              // 如 "gpt-4", "claude-3"
  status: 'idle' | 'running' | 'error';
  createdAt: string;
  updatedAt: string;
}

// 消息
export interface Message {
  id: string;
  role: 'user' | 'assistant' | 'system' | 'tool';
  content: string;
  createdAt: string;
  attachments?: Attachment[];
}

export interface Attachment {
  type: 'image' | 'file' | 'audio';
  url: string;
  name: string;
}

// 任务
export interface Task {
  id: string;
  name: string;
  description: string;
  cron: string;              // cron 表达式
  status: 'idle' | 'running' | 'error';
  nextRunAt: string;
  lastRunAt: string;
  lastStatus: 'success' | 'error';
  consecutiveErrors: number;
}

// 工具
export interface Tool {
  id: string;
  name: string;
  description: string;
  schema: Record<string, unknown>;  // JSON Schema
}

// WebSocket 消息
export interface WSMessage {
  type: 'message' | 'typing' | 'tool_call' | 'tool_result' | 'task_complete';
  payload: unknown;
  sessionId?: string;
  taskId?: string;
}
```

---

## 4. API 服务层

```typescript
// services/api.ts
const API_BASE = import.meta.env.VITE_API_BASE || 'http://localhost:8000/api';

async function request<T>(
  path: string,
  options: RequestInit = {}
): Promise<T> {
  const token = localStorage.getItem('token');
  
  const res = await fetch(`${API_BASE}${path}`, {
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...(token ? { Authorization: `Bearer ${token}` } : {}),
      ...options.headers,
    },
  });

  if (!res.ok) {
    throw new Error(`API Error: ${res.status}`);
  }

  return res.json();
}

// services/agent.ts
import { request } from './api';
import type { Agent, Message } from '../types';

export const agentApi = {
  list: () => request<Agent[]>('/agents'),
  
  get: (id: string) => request<Agent>(`/agents/${id}`),
  
  create: (data: Partial<Agent>) => 
    request<Agent>('/agents', { method: 'POST', body: JSON.stringify(data) }),
  
  delete: (id: string) => 
    request<void>(`/agents/${id}`, { method: 'DELETE' }),
  
  chat: (agentId: string, message: string) =>
    request<Message>(`/agents/${agentId}/chat`, {
      method: 'POST',
      body: JSON.stringify({ message }),
    }),
};
```

---

## 5. Zustand 状态管理

```typescript
// stores/chatStore.ts
import { create } from 'zustand';
import type { Message } from '../types';

interface ChatState {
  messages: Message[];
  isLoading: boolean;
  currentAgentId: string | null;
  
  addMessage: (msg: Message) => void;
  setLoading: (loading: boolean) => void;
  clearMessages: () => void;
  setCurrentAgent: (id: string) => void;
}

export const useChatStore = create<ChatState>((set) => ({
  messages: [],
  isLoading: false,
  currentAgentId: null,

  addMessage: (msg) =>
    set((state) => ({ messages: [...state.messages, msg] })),

  setLoading: (loading) => set({ isLoading: loading }),

  clearMessages: () => set({ messages: [] }),

  setCurrentAgent: (id) => set({ currentAgentId: id }),
}));
```

---

## 6. WebSocket 实时通信

```typescript
// hooks/useWebSocket.ts
import { useEffect, useRef, useCallback } from 'react';
import type { WSMessage } from '../types';

export function useWebSocket(
  url: string,
  onMessage: (msg: WSMessage) => void
) {
  const wsRef = useRef<WebSocket | null>(null);

  const connect = useCallback(() => {
    wsRef.current = new WebSocket(url);

    wsRef.current.onopen = () => {
      console.log('WebSocket 已连接');
    };

    wsRef.current.onmessage = (event) => {
      const data: WSMessage = JSON.parse(event.data);
      onMessage(data);
    };

    wsRef.current.onerror = (err) => {
      console.error('WebSocket 错误:', err);
    };

    wsRef.current.onclose = () => {
      console.log('WebSocket 已断开，3秒后重连...');
      setTimeout(connect, 3000);
    };
  }, [url, onMessage]);

  useEffect(() => {
    connect();
    return () => wsRef.current?.close();
  }, [connect]);

  const send = (data: unknown) => {
    wsRef.current?.send(JSON.stringify(data));
  };

  return { send };
}
```

---

## 7. 组件设计

### 7.1 AgentChat 页面核心结构

```tsx
// pages/AgentChat/index.tsx
import { useState } from 'react';
import { useChatStore } from '../../stores/chatStore';
import { agentApi } from '../../services/agent';
import { MessageList } from './MessageList';
import { ChatInput } from './ChatInput';
import { ToolCallPanel } from './ToolCallPanel';

export function AgentChat() {
  const { messages, addMessage, setLoading, currentAgentId } = useChatStore();
  const [showTools, setShowTools] = useState(false);

  const handleSend = async (content: string) => {
    if (!currentAgentId) return;

    // 添加用户消息
    addMessage({
      id: crypto.randomUUID(),
      role: 'user',
      content,
      createdAt: new Date().toISOString(),
    });

    setLoading(true);

    try {
      const response = await agentApi.chat(currentAgentId, content);
      addMessage(response);
    } catch (error) {
      addMessage({
        id: crypto.randomUUID(),
        role: 'assistant',
        content: `出错了：${error}`,
        createdAt: new Date().toISOString(),
      });
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="flex flex-col h-screen">
      <MessageList messages={messages} />
      <ChatInput onSend={handleSend} />
      {showTools && <ToolCallPanel onClose={() => setShowTools(false)} />}
    </div>
  );
}
```

### 7.2 任务监控页面

```tsx
// pages/TaskMonitor/index.tsx
import { useQuery } from '@tanstack/react-query';
import { taskApi } from '../../services/task';
import { formatDistanceToNow } from 'date-fns';

export function TaskMonitor() {
  const { data: tasks, isLoading } = useQuery({
    queryKey: ['tasks'],
    queryFn: taskApi.list,
    refetchInterval: 10000,  // 每10秒刷新
  });

  if (isLoading) return <div>加载中...</div>;

  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold mb-6">任务监控</h1>
      
      <div className="space-y-4">
        {tasks?.map((task) => (
          <div 
            key={task.id} 
            className={`p-4 rounded-lg border ${
              task.status === 'error' ? 'border-red-500 bg-red-50' :
              task.status === 'running' ? 'border-blue-500 bg-blue-50' :
              'border-gray-200'
            }`}
          >
            <div className="flex justify-between items-center">
              <div>
                <h3 className="font-semibold">{task.name}</h3>
                <p className="text-sm text-gray-500">{task.description}</p>
              </div>
              <span className={`px-3 py-1 rounded-full text-sm ${
                task.status === 'error' ? 'bg-red-100 text-red-700' :
                task.status === 'running' ? 'bg-blue-100 text-blue-700' :
                'bg-gray-100 text-gray-700'
              }`}>
                {task.status === 'running' ? '运行中' : 
                 task.status === 'error' ? '错误' : '空闲'}
              </span>
            </div>
            
            <div className="mt-2 text-sm text-gray-400">
              下次运行：{formatDistanceToNow(new Date(task.nextRunAt), { addSuffix: true })}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
```

---

## 8. 环境变量配置

```bash
# .env
VITE_API_BASE=http://localhost:8000/api
VITE_WS_BASE=ws://localhost:8000/ws
VITE_APP_NAME=AI Agent Platform
```

---

*本周任务：完成前端项目初始化 + API 层 + 核心页面*
